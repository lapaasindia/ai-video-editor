# AI Video Editor - Environment Variables

# ── LLM Provider Priority Chain ─────────────────────────────────────────────
# The system auto-detects the best available provider in this order:
#   1. Codex CLI  (needs OPENAI_API_KEY + `codex` binary or npx @openai/codex)
#   2. OpenAI API (needs OPENAI_API_KEY)
#   3. Google Gemini (needs GOOGLE_API_KEY)
#   4. Anthropic Claude (needs ANTHROPIC_API_KEY)
#   5. Ollama local (always available as fallback — no key needed)
#
# To force a specific provider, set LAPAAS_LLM_PROVIDER and LAPAAS_LLM_MODEL:
#   LAPAAS_LLM_PROVIDER=ollama
#   LAPAAS_LLM_MODEL=qwen3:1.7b
LAPAAS_LLM_PROVIDER=
LAPAAS_LLM_MODEL=

# LLM API Keys
OPENAI_API_KEY=
GOOGLE_API_KEY=
ANTHROPIC_API_KEY=

# Asset Providers (Optional - for auto-fetching stock media)
# Get keys from:
# Pexels: https://www.pexels.com/api/
# Pixabay: https://pixabay.com/api/docs/
# Unsplash: https://unsplash.com/developers
PEXELS_API_KEY=
PIXABAY_API_KEY=
UNSPLASH_API_KEY=

# Backend Configuration (Optional)
LAPAAS_DESKTOP_PORT=43123
